#!/bin/sh
# Runs the gate before every commit. The credential scan is the reason: this
# project handled four live API keys during development and one nearly reached
# a file. Bypass with --no-verify only if you know exactly why.
set -e
cd "$(git rev-parse --show-toplevel)/sdks/node"
bun run gate
